export type UserType = {
  email: string;
  password: string;
};

export type PostType = {
  title: string;
  category: string;
  description: string;
  isPublic: string;
  lat: string;
  lng: string;
};
